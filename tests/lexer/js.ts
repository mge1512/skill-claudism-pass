// A comment worth noting at the top.
const re = /\/\*not a comment\*\//g;
const s = "he said // nothing";
const t = `a template
spanning lines with // no comment`;
const q = str.replace(/'/g, "");   // this trailing one is groundbreaking
const div = a / b / c;             /* block comment, at the end of the day */
const y = total / count; // this trailing comment must survive the division
